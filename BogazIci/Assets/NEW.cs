using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NEW : MonoBehaviour
{
    public string denemeler;
    // Start is called before the first frame update
    void Start()
    {
        iננננ
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log(denemeler);
        print("x");
    }
}
